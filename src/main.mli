(** Main entry point for the obuild executable

    This module implements the command-line interface for obuild,
    including all command handlers (configure, build, clean, etc.)
    and the main dispatch logic.

    No public interface is exposed - this module executes on startup.
 *)
